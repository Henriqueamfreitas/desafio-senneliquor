import styled from "styled-components"

export const StyledChamadoList = styled.ul`
    margin: 0rem 5rem;
    padding-top: 2rem;
    background-color: var(--color-white);
    display: flex;
    overflow-x: auto;
    max-width: calc(100vw - 10rem);
    width: calc(100vw - 10rem);
    min-width: calc(100vw - 10rem);
`